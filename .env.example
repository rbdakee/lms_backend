# Скопировать в .env и поправить под своё окружение. Значения по умолчанию
# подходят для локальной разработки с docker compose — файл можно не создавать.

ENV=dev
DATABASE_URL=postgresql+psycopg://lms:lms@localhost:5445/lms

# Через запятую. В бою — https://domain.kz,https://admin.domain.kz
CORS_ORIGINS=["http://localhost:3000","http://localhost:3001"]

# В бою — .domain.kz, чтобы кука покрывала оба фронта. Пусто = текущий хост.
COOKIE_DOMAIN=
COOKIE_SECURE=false

# ВОПРОС НЕ РЕШЁН — решить до боя, см. DEPLOY.md, «Адрес клиента».
# На адресе клиента держатся суточный потолок SMS и потолок публичной
# проверки сертификата. За nginx `false` означает, что все запросы приходят
# с адреса прокси: 30 SMS в сутки и 20 проверок в минуту НА ВСЮ СТРАНУ.
# А `true` сейчас берёт первый элемент X-Forwarded-For, который клиент
# дописывает сам, — то есть ведро он выбирает себе тоже сам.
# Оба значения неверны; строчку оставляем закомментированной нарочно,
# чтобы деплой «по образцу» не сделал молчаливый выбор.
# TRUST_FORWARDED_FOR=false

# Провайдер SMS. Значение сегодня одно — `log`: заглушка пишет код входа
# в лог, настоящего адаптера ещё нет. Любое другое значение сервис
# не поднимет — так же, как неизвестные STORAGE_PROVIDER и TELEGRAM_PROVIDER.
SMS_PROVIDER=log

# Приватное хранилище файлов. Каталог создаётся сам, в git не уходит.
STORAGE_PROVIDER=local
STORAGE_DIR=var/storage
# Тот же секрет вписывается в secure_link_md5 nginx: подпись должна сходиться
# у бэкенда и у раздатчика. В бою — длинная случайная строка.
STORAGE_SECRET=dev-secret
# Адрес, с которого фронт скачивает файл. В бою — https://api.domain.kz
PUBLIC_BASE_URL=http://localhost:8000
# Куда ведёт QR с бумажного сертификата: адрес КЛИЕНТСКОГО приложения,
# где живёт страница /verify. В бою — https://domain.kz, а не адрес API.
# Пусто — QR на сертификате не печатается.
VERIFY_BASE_URL=http://localhost:3000

# Telegram-бот админа. Заглушка `log` пишет уведомления в лог и никуда не ходит;
# `bot` — настоящая отправка. Токен и имя бота выдаёт @BotFather.
TELEGRAM_PROVIDER=log
TELEGRAM_BOT_TOKEN=
TELEGRAM_BOT_USERNAME=
# Секрет вебхука — то же значение, что уходит в setWebhook параметром
# secret_token: только он отличает запрос Telegram от чужого. Пусто —
# вебхук отвергает всё.
TELEGRAM_WEBHOOK_SECRET=
