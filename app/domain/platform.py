"""Коды учебных площадок.

Слаги нейтральные, а не имена брендов: бренд переименуют, а колонка
`platform` в девяти таблицах и в ключах переживёт переименование без
миграции (PLATFORMS_BRIEF, решение 7).

Первая площадка — та, что работала до разделения: её код получают все
строки, заведённые раньше, и запрос с неизвестного источника.
"""

PLATFORMS = ("p1", "p2")

# Запрос без Origin или с чужим — это curl, тесты и пропущенная настройка.
# Отказывать им дороже, чем ответить как первой площадке.
DEFAULT_PLATFORM = PLATFORMS[0]
